spring:
  application:
    name: registry
  cloud:
    config:
      uri: http://piggymetrics-config.mybluemix.net
      fail-fast: true
      password: user
      username: user

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0
